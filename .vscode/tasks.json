{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "Live Preview",
			"detail": "Load on startup to remind that preview is available",
			"workspacePath": "${workspaceFolder}",
			"label": "Live Preview: Run Server",
			"runOptions": {
				"runOn": "folderOpen"
			}
		}
	]
}