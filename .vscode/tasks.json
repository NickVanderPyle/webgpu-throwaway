{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "LivePreview on Load",
            "type": "process",
            "command": "${input:livepreview}",
            "runOptions": {
                "runOn": "folderOpen"
            }
        }
    ],
    "inputs": [
        {
            "id": "livepreview",
            "type": "command",
            "command": "livePreview.start"
        }
    ]
}